/**
 * ThemedActivityIndicatorColors.ts
 *
 * Defines color keys for light and dark modes of the activity indicator.
 * These will be merged into your global Colors object, so that
 * ThemedActivityIndicator can access them via useThemeColor.
 */

const ThemedActivityIndicatorColors = {
    light: {
      // Default ring color for light mode
      activityIndicatorColor: "#4b9cdb",
    },
    dark: {
      // Default ring color for dark mode
      activityIndicatorColor: "#2980b9",
    },
  };
  
  export default ThemedActivityIndicatorColors;
  